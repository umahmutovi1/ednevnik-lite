# Flask-Migrate / Alembic — initialized by: flask db init
#
# After cloning the repo, run:
#   flask db init        (once — creates the migrations/ folder)
#   flask db migrate     (generates a migration from models.py)
#   flask db upgrade     (applies the migration to the DB)
#
# Then seed data:
#   flask seed-roles
#   flask seed-admin
#
# This file is a placeholder. The actual Alembic env.py and versions/
# directory are created by `flask db init`.
