# =============================================================================
# Dnevnik-Lite — requirements.txt (Phase 2)
# [OWASP A03:2025 – Software Supply Chain Failures]
#
# ALL versions pinned. Annotate any dep with known CVE history.
#
# HOW TO AUDIT:
#   pip install pip-audit
#   pip-audit --requirement requirements.txt --severity-threshold high
#
# pip-audit vs Dependabot:
#   pip-audit  : scans for KNOWN CVEs in installed versions
#   Dependabot : alerts when newer versions exist (regardless of CVE)
#   Both are needed — neither alone is sufficient.
# =============================================================================

# --- Core Framework ---
Flask==3.0.3
# [SUPPLY CHAIN NOTE]: Flask had CVE-2023-30861 (open redirect). Fixed in 2.3.2+. 3.0.3 is patched.

# --- ORM & Database ---
Flask-SQLAlchemy==3.1.1     # ORM — SQL Injection structurally impossible [OWASP A05:2025]
Flask-Migrate==4.0.7        # Alembic schema version control
psycopg2-binary==2.9.9
# [SUPPLY CHAIN RISK]: binary wheel bypasses source verification. Use psycopg2 (non-binary) in prod.

# --- Authentication & Token Security ---
Flask-JWT-Extended==4.6.0   # Stateless JWT — short-lived tokens + rotation [OWASP A07:2025]
flask-bcrypt==1.0.1         # bcrypt work factor 12 [OWASP A04:2025]
itsdangerous==2.2.0         # HMAC-signed tokens for password reset flows

# --- Rate Limiting ---
Flask-Limiter==3.7.0        # Per-IP rate limiting [OWASP A07:2025]
redis==5.0.4
# [SUPPLY CHAIN NOTE]: redis-py had CVE-2023-28858 (SSRF). Fixed in 4.5.4+. 5.0.4 is patched.

# --- Security Headers & HTTPS ---
Flask-Talisman==1.1.0       # HTTPS + HSTS + CSP + security headers [OWASP A02:2025]

# --- Input Validation ---
marshmallow==3.21.3         # Schema validation — all POST/PATCH endpoints [OWASP A05:2025]

# --- Environment & Config ---
python-dotenv==1.0.1        # Loads secrets from .env — never hardcode [OWASP A02:2025]

# --- CORS ---
Flask-Cors==4.0.1           # Locked to specific origins in prod [OWASP A02:2025]

# --- Testing ---
pytest==8.2.2
pytest-flask==1.3.0
pytest-cov==5.0.0
factory-boy==3.3.0
